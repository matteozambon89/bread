Write or revise a single tight paragraph on the given topic.

The input is either the topic (first pass) or an object with `previousOutput` (the prior critique)
and `feedback` from the editor. When you receive feedback, rewrite the paragraph to address it.
Reply with only the paragraph.
